server:
  port: 7900
spring:
  application:
    name: cloud-conf
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: file:E:/show/ppx/confs/loc-prv